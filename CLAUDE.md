# HW Lab 網站 — 專案說明

HW 的對外網站。任何 AI 接手前先讀完這份。

## 基本資料

| | |
|---|---|
| 線上網址 | https://huawei522-commits.github.io/hw-lab/philo/ |
| GitHub | https://github.com/huawei522-commits/hw-lab（public） |
| 這個資料夾 | **正本**。git 倉庫 |
| 部署 | 推上 main 之後 GitHub Pages 自動建置，一到兩分鐘生效 |

**這個資料夾刻意不放在 Google 雲端硬碟裡。** 雲端同步會在 git 寫入到一半時插手，
是已知會弄壞版本紀錄的組合。不要把它搬進雲端硬碟。

## 檔案結構

```
index.html          自動轉址到 philo/（原本的索引頁已撤除）
og-cover.jpg        LinkedIn／社群分享用的預覽圖 1200×630
philo/index.html    哲學探索地圖，整個網站的本體（單一檔案，含全部資料與程式）
philo/img/          86 張 WebP 圖片（原始 PNG 在雲端硬碟的 HW_哲學/素材圖/）
```

## 改完之後

```bash
git -C "C:/Users/huawei/hw-lab-site" add -A
git -C "C:/Users/huawei/hw-lab-site" commit -m "說明"
git -C "C:/Users/huawei/hw-lab-site" push
```

推完等一到兩分鐘。**HW 常常反映「看不到改動」，九成是瀏覽器快取**——
請她按 Ctrl + Shift + R 強制重新整理。

## 改動時一定要遵守的事

**1. 內容的定位是「從當代 AI 爭論回看哲學史」**，不是通用哲學百科。
標題、導言、新增內容都照這個定位走。

**2. 引文與觀點摘要要分清楚。**
`QUOTES` 有 67 筆，其中 `PARAPHRASE` 這個 Set 列出的 32 筆**不是逐字引文**，
呈現時不加引號、標示為「觀點摘要」。新增時判準偏保守：
**不確定是不是原話，就歸為觀點摘要。** 宣稱某人說過某句話的風險比較高。

**3. 中英對照的英文是次要層。** 小一級、淡色、義式斜體，用 `.lat` 這個 class。
目前有英文的是：頁籤、8 個領域、7 個時代、67 位姓名、22 組交鋒題目、12 道大哉問題目。
內文（`one`、`bio`、`why`、`stance`、答案）維持中文，這是刻意的，不是還沒做完。

**4. 手機與桌機的關聯圖譜行為不同**，改 CSS 或事件時別打破：
- `CAN_HOVER`（`matchMedia("(hover:hover)")`）決定走哪一套
- 桌機：滑過亮關聯、點一下開個人頁
- 觸控：第一次點亮關聯並浮出名字，第二次點同一個才開個人頁（狀態在 `tapped`）
- 窄螢幕人名 `display:none`，靠 `.focus` / `.near` 浮出
- `.gnode.focus circle` 這類規則**必須加 `:not(.hit)`**，
  否則會把當作觸控範圍的透明大圓一起縮小（踩過一次）
- `fitGraph()` 只縮不放（上限 1），放大會讓桌機排版變擠

## 驗證（很重要）

**大批次資料手術不能只看腳本印出來的統計數字。** 這個專案踩過三個坑，
全都是腳本回報「成功」但資料其實已經壞掉：

1. 正則 `[^"]|\"` 等於「任何字元」，貪婪吃掉相鄰資料，誤刪 4 句名言
2. 用 split 重組陣列時第一格是空字串，產生 `[ , {...}]` 空洞，整頁崩潰
3. 用「不重複 id 數」驗證，集合運算把重複蓋掉，沒發現 QUOTES 多了 4 筆

→ 驗證時要同時檢查「有沒有缺」**和「有沒有多」**，
並且一定要在瀏覽器實跑、看主控台有沒有錯誤。

改完至少確認：`T.length`、`TENSIONS.length`、`QUESTIONS.length`、
`Object.keys(QUOTES).length`、`Object.keys(DEEP).length` 這五個數字彼此對得上，
而且陣列裡沒有空洞（`T.some(t=>!t)` 要是 false）。

## 雲端硬碟那邊

`G:\我的雲端硬碟\00_RAG\HW_哲學\` 有一份**快照**（`HW哲學探索地圖.html`）
給 HW 離線開啟用，不是正本。改完正本要記得同步過去，
同步時圖片路徑要從 `img/*.webp` 改回 `素材圖/*.png`。
那個資料夾的 `AGENTS.md` 有更完整的專案背景。

決策紀錄與內容檢討報告在 `00_RAG/113_claude網站研究/`。

## 現況數字

67 位思想家｜22 張思想交鋒｜12 道大哉問｜8 個領域｜7 個時代｜134 條關聯線
